{
  "name": "aurelis",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "npm run gen && astro build",
    "preview": "astro preview",
    "gen": "node scripts/emit-tokens.mjs && node scripts/generate-field.mjs && node scripts/generate-plans.mjs",
    "test": "node --test 'tests/**/*.test.mjs'",
    "start": "npm run build && npm run serve",
    "serve": "astro preview --port 4321 --host"
  },
  "dependencies": {
    "astro": "^5.18.2"
  },
  "devDependencies": {
    "playwright": "^1.63.0",
    "@axe-core/playwright": "^4.13.0",
    "axe-core": "^4.13.0",
    "sharp": "^0.35.4"
  }
}
