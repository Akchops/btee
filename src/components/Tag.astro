---
interface Props { label?: string; class?: string; }
const { label, class: cls = '' } = Astro.props;
---
<span class={`tag ${cls}`}><svg viewBox="0 0 8 10" aria-hidden="true" focusable="false"><path d="M1 9V1h6"/></svg>{label && <span>{label}</span>}<slot /></span>
